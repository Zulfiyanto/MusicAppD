{{!-- Header --}}
{{> header}}
  <body>
    <div class="container-fluid container-css">
      <div class="row">
        {{! column music }}
        {{> music}}
        {{! Column Playlist }}
      </div>
    </div>
    {{> modal}}
    {{!-- script --}}
    {{> scriptJS}}
  </body>
</html>