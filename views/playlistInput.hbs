{{!-- Header --}}
{{> header}}
  <body>
    <div class="container-fluid container-css">
      <div class="row">
        {{! column music }}
        {{> playlistInput}}
        {{! Column Playlist }}
        {{> playlistSection}}
      </div>
    </div>
    {{> modal}}
    {{!-- script --}}
    {{> scriptJS}}
  </body>
</html>